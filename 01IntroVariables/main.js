      // cia rasomas js kode
      5 + 5;

      // sukuriame kintmaji
      // = priskiria desne puse prie kairiai
      let vardas = "James";

      const h1El = document.createElement('h1');
      // pridedam jam teksitine reiksme
      h1El.textContent = vardas;
      // patalpinu i body taga
      document.body.append(h1El);

      // pakeisti dydi
      h1El.style.fontSize = '5rem'

      //vardas = 'Mike'
      //vardas = 500
      //vardas = true

      console.log(5 + 5);
      console.log(vardas);

      let ;